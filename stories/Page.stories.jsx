import React from 'react';

import Page from '@react-examples/page';

export default {
  title: 'Example/Page',
  component: Page,
};

const Template = (args) => <Page {...args} />;

export const PageComponent = Template.bind({});
